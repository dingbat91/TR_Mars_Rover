# The Grid Class

The Grid Class contains all details of the Map Grid itself.

The Map Grid is an array of Map Grid Square, which contain the data of what is in the square.Currently it has one terrain feature (a mountain), and the Rover itself. IT also contains the displayGrid function which is used to display the grid on screen.

Further implementation will show a symbol for the highest priority object (The Rover being top)
